server:
  port: 8082
  servlet:
    context-path: /department-service

spring:
  application:
    name: onthick-department-service
  datasource:
    url: jdbc:mysql://localhost:3306/onthick_ktpm
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        ddl-auto: update
    generate-ddl: true

